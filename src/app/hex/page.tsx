import HexagonChart from "@/components/hexagon-chart";

export default function HexPage() {
  return (
    <main style={{ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", background: "#faf9fb" }}>
      <HexagonChart />
    </main>
  );
}
